### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Auth {
  token: String!
  user: User!
}

type Card {
  categoryId: Int
  details: String!
  done: Boolean!
  id: Int!
  title: String!
  user: User
}

type Category {
  cards: [Card]!
  id: Int!
  name: String!
  user: User
}

type Mutation {
  createCard(category: String!, details: String!, title: String!): Card!
  createCategory(name: String!): Category!
  deleteCard(id: Int!): Card!
  login(email: String!, password: String!): Auth!
  markDone(id: Int!): Card!
  signup(email: String!, name: String!, password: String!): Auth!
  updateCard(details: String, id: Int!, title: String): Card!
}

type Query {
  allCards: [Card!]!
  allCategories: [Category!]!
}

type User {
  cards: [Card]!
  categories: [Category]!
  email: String!
  id: Int!
  name: String!
  password: String!
}