### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Auth {
  token: String!
  user: User!
}

type Card {
  categoryName: String
  createdAt: DateTime!
  details: String!
  done: Boolean!
  id: Int!
  title: String!
  user: User
}

type Category {
  cards: [Card]!
  id: Int!
  name: String!
  user: User
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

input LinkOrderByInput {
  createdAt: Sort
  details: Sort
  title: Sort
}

type Mutation {
  createCard(category: String!, details: String!, title: String!): Card!
  createCategory(name: String!): Category!
  deleteCard(id: Int!): Card!
  login(email: String!, password: String!): Auth!
  markDone(id: Int!): Card!
  readOneCard(id: Int!): Card!
  signup(email: String!, name: String!, password: String!): Auth!
  updateCard(details: String, id: Int!, title: String): Card!
}

type Query {
  allCards(filter: String, orderBy: [LinkOrderByInput!], skip: Int, take: Int): [Card!]!
  allCategories: [Category!]!
}

enum Sort {
  asc
  desc
}

type User {
  cards: [Card]!
  categories: [Category]!
  email: String!
  id: Int!
  name: String!
  password: String!
}